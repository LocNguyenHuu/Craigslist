<div class="container">
  <h1> All Categories</h1>

  <div class="col-sm-10 col-sm-offset-1 col-xs-12 blog-content">
    <% @categories.each do |category| %>
    <div class="col-xs-12">
      <div class="text-center">
        <h2><a href="/categories/<%=category.id%>"</a><%= category.category %></h2>
      </div>
      <br>
    </div>
    <% end %>
  </div>
</div>
